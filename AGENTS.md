# Working in this repo

The engineering rules — the six that keep the packaging as honest as the code, plus the pre-finish
checklist — live in [`CLAUDE.md`](CLAUDE.md) under **"Engineering rules — no AI slop."** Read them before
writing code. They are the single source of truth; this file only points at them (per Rule 4: don't keep two
copies that can drift).

Architecture, the data-driven engine principle, and the commands are in the rest of `CLAUDE.md`;
[`docs/architecture.md`](docs/architecture.md) has the design.
